{
  "name": "mementomorium-website",
  "version": "1.0.0",
  "private": true,
  "description": "Tooling for CI checks on the Mementomorium band website. The site itself is plain HTML/CSS/JS and needs no build step to run.",
  "scripts": {
    "lint:html": "html-validate index.html",
    "lint:css": "stylelint \"css/**/*.css\"",
    "lint:js": "eslint \"js/**/*.js\"",
    "lint": "npm run lint:html && npm run lint:css && npm run lint:js"
  },
  "devDependencies": {
    "eslint": "^9.12.0",
    "html-validate": "^8.24.0",
    "stylelint": "^16.9.0",
    "stylelint-config-standard": "^36.0.1"
  }
}
