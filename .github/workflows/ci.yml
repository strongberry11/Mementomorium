name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint:
    name: Build & Lint (Astro / Tailwind / CSS / JS)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build (astro build)
        run: npm run build

      - name: Type-check Astro components
        run: npm run check

      - name: Lint CSS (stylelint)
        run: npm run lint:css

      - name: Lint JS/Astro (eslint)
        run: npm run lint:js

      - name: Lint built HTML (html-validate)
        run: npm run lint:html

  link-check:
    name: Check links & anchors
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build (astro build)
        run: npm run build

      - name: Lychee link check
        uses: lycheeverse/lychee-action@v2
        with:
          args: >-
            --no-progress
            --root-dir ./dist
            --exclude "https://www.instagram.com/.*"
            --exclude "https://www.tiktok.com/.*"
            --accept 200,206,429
            --max-retries 3
            --timeout 20
            dist/index.html
          fail: true

  lighthouse:
    name: Lighthouse (Performance / A11y / Best Practices / SEO)
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Build (astro build)
        run: npm run build

      - name: Serve site
        run: npx --yes http-server dist -p 8080 --silent &

      - name: Wait for local server
        run: npx --yes wait-on http://127.0.0.1:8080/index.html

      - name: Run Lighthouse CI
        uses: treosh/lighthouse-ci-action@v11
        with:
          urls: http://127.0.0.1:8080/index.html
          configPath: ./.lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
